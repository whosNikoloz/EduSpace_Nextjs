import React, { FC } from "react";
import { Button, Avatar } from "@nextui-org/react";

interface UserEditProps {
  username: string;
}

export const UserEdit: FC<UserEditProps> = ({ username }) => {
  return <></>;
};
